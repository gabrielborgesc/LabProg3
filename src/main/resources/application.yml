server:
  port: 8081

spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .html

  datasource:
    url: "jdbc:postgresql://localhost:5432/labprog?currentSchema=chat"
    username: postgres
    password: postgres

  jpa:
    database: postgresql
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
